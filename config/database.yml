default: &default
  adapter: postgresql
  encoding: unicode #utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: lukask_user
  password: 123PreLukaskPay
  timeout: 5000
  port: 5432

development:
  <<: *default
  database: production_api #db/development.sqlite3

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: test_api  #db/test.sqlite3

production:
  <<: *default
  database: production_api #db/production.sqlite3
